on: [push]

jobs:
  test_job:
    runs-on: ubuntu-latest
    name: Linting and Testing
    steps:
      - name: Checkout
        uses: actions/checkout@v2
      - name: Test action step
        uses: ./
        id: test

